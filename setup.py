from pathlib import Path

Path('flats-data').mkdir(parents=True, exist_ok=True)
Path('flats-data/listings').mkdir(parents=True, exist_ok=True)
Path('flats-data/urls').mkdir(parents=True, exist_ok=True)